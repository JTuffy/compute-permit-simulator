"""Core domain constants for the Compute Permit Simulator.

DEFAULT_* parameter values have moved to ``schemas.defaults`` so that
the schemas layer doesn't import from core.

This module is intentionally empty — it exists as a reminder of the
scaling reference documentation now maintained in ``schemas/defaults.py``.
"""
